extends base

include mixins

append article
  article append
  +foo("a", "b")
    h1 1

mixin foo()
  p footer-foo

prepend footer
  article prepend
  +foo()(do="not care")
    h2 2
  +bar().biz
    h3 3
